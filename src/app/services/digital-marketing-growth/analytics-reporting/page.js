import PageClient from './PageClient';

export const metadata = {
  title: 'Analytics & Reporting | GA4 Setup & Custom Dashboards',
  description: 'We handle GA4 setup, custom dashboards, monthly reports, complete attribution modeling, e-commerce tracking services to deliver real client value.',
  openGraph: {
    title: 'Analytics & Reporting | GA4 Setup & Custom Dashboards',
    description: 'We handle GA4 setup, custom dashboards, monthly reports, complete attribution modeling, e-commerce tracking services to deliver real client value.',
  },
};

export default function Page() {
  return <PageClient />;
}
