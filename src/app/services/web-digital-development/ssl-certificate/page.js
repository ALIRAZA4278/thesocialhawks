import PageClient from './PageClient';

export const metadata = {
  title: 'SSL Certificate Installation | HTTPS Setup for Your Website',
  description: 'Get installation services for SSL/TLS certificates including DV OV EV and wildcard certificates while we prepare your website to run using HTTPS.',
  openGraph: {
    title: 'SSL Certificate Installation | HTTPS Setup for Your Website',
    description: 'Get installation services for SSL/TLS certificates including DV OV EV and wildcard certificates while we prepare your website to run using HTTPS.',
  },
};

export default function Page() {
  return <PageClient />;
}
