import PageClient from './PageClient';

export const metadata = {
  title: 'Domain Registration & Strategy | Get the Right Domain Name',
  description: 'We assist you in selecting the right domain, renewals management, handle registration and set up WHOIS privacy protection.',
  openGraph: {
    title: 'Domain Registration & Strategy | Get the Right Domain Name',
    description: 'We assist you in selecting the right domain, renewals management, handle registration and set up WHOIS privacy protection.',
  },
};

export default function Page() {
  return <PageClient />;
}
