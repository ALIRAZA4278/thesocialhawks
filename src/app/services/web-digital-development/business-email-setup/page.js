import PageClient from './PageClient';

export const metadata = {
  title: 'Business Email Setup | Google Workspace & Microsoft 365',
  description: 'Get a professional email address with your domain name. We set up Google Workspace, Microsoft 365, or Zoho — DNS records, migration, mobile config, all of it.',
  openGraph: {
    title: 'Business Email Setup | Google Workspace & Microsoft 365',
    description: 'Get a professional email address with your domain name. We set up Google Workspace, Microsoft 365, or Zoho — DNS records, migration, mobile config, all of it.',
  },
};

export default function Page() {
  return <PageClient />;
}
